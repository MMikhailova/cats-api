<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat finder</title>
</head>
<body>
   <div>
<% if(cats){ %>
    <% cats.forEach((cat) => { %>

        <div>
            <h1><%= cat.name %></h1>
        </div>
        <% }) %>
    <% } %>
    </div>
</body>
</html>